{
  "name": "rest-and-rooms-lambda-firebase-auth",
  "version": "1.0.0",
  "description": "Rest and rooms lambda firebase auth api",
  "main": "src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "zip -FSr build.zip node_modules src/*"
  },
  "author": "Luis Giraldo",
  "license": "ISC",
  "dependencies": {
    "firebase-admin": "^8.12.1"
  }
}
